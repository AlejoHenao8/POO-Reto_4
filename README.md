# POO-Reto_4
This repository is a modification of the POO-Reto_3 repository, adding setters and getters and overriding `total_price()` according to the order composition.  This repository is a modification of the POO-Reto_3 repository, adding setters and getters and overriding `total_price()` according to the order composition.
